{
  "name": "aistudio-container-extension",
  "private": true,
  "version": "0.3.3",
  "scripts": {
    "pretest": "go run ../../cmd/build-extension -source . -config ../../config/upstream.yaml",
    "test": "node --test tests/*.test.cjs"
  }
}
